import { css } from "lit";

export const materialIconStyles = css`
    .material-icons {
        font-family: 'Material Icons';
        font-size: 24px;
        vertical-align: middle;
        cursor: pointer;
    }
`